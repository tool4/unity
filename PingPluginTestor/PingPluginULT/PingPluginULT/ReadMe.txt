========================================================================
    CONSOLE APPLICATION : Ping Plugin ULT
========================================================================

Unit level test file for PingPlugin - loads the shared dll/so and runs
few example commands

/////////////////////////////////////////////////////////////////////////////
